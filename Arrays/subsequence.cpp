#include <iostream>
using namespace std;


//subsequence is a sequence that can be derived from an array by selecting zero or more random elements, without changing the order of the remaining elements.

// no. of subarrays of array with n elements = 2^n

int main(){
    
    return 0;
}